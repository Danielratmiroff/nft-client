import React, { FC, KeyboardEvent, useState } from "react";
import Logo from "../Logo";

const Navbar: FC = () => {
  return (
    <div>
      <Logo />
      This is a navbar
    </div>
  );
};

export default Navbar;
